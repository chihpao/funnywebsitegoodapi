# funnywebsitegoodapi

IT鐵人gogo

## 推上github的步驟

1. **檢查當前狀態**
   git status
2. **添加修改的文件**
   git add .
3. **提交修改**
   git commit -m "修改了什麼"
4. **推送到遠端倉庫(假設分支名稱是 main，如果是其他分支名稱，替換 main 為其他分支名稱)**
   git push origin main
